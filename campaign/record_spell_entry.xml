<?xml version="1.0" encoding="iso-8859-1"?>

<!-- Please see the LICENSE.md file included with this distribution for attribution and copyright information. -->

<root>
	<template name="spell_action_castbutton" merge="join">
		<button_spellaction name="castbutton">
			<script>
				function action(draginfo)
					SpellFailure.arcaneSpellFailure(window.getDatabaseNode().getChild('...'))
					SpellManager.onSpellAction(draginfo, window.getDatabaseNode());
				end
				
				function onButtonPress()
					action();
				end
				
				function onDragStart(button, x, y, draginfo)
					action(draginfo);
					return true;
				end
			</script>
		</button_spellaction>
	</template>
</root>